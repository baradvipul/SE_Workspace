#include <stdio.h>

int main() {
    int i, j, num = 1;

    // 4 rows (since 1+2+3+4 = 10 numbers)
    for (i = 1; i <= 4; i++) {
        for (j = 1; j <= i; j++) {
            printf("%d", num);
            num++;
        }
        printf("\n");
    }
    return 0;
}
