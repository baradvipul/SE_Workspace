#include <stdio.h>
#include <math.h>  // Required for sqrt()

int main() {
    double num = 16.0;
    double result = sqrt(num);  // Predefined function
    printf("Square root of %.2f = %.2f\n", num, result);
    return 0;
}
